<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="Puscas_Andrei_Lab3.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:Puscas_Andrei_Lab3"
    Shell.FlyoutBehavior="Disabled">
 
    <TabBar>
        <ShellContent Title="Welcome" ContentTemplate="{DataTemplate local:MainPage}"/>
        <ShellContent Title="About" ContentTemplate="{DataTemplate local:AboutPage}"/>
        <ShellContent Title="Add Shopping List" ContentTemplate="{DataTemplate local:ListEntryPage}"/>
    </TabBar>

</Shell>
